/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: [""],
  },
  // api: {
  //   bodyParser: false,
  // },
};

module.exports = nextConfig;
